import React from 'react'
import { useFetch } from '../utils/api';

export default function Search() {

    // const endpoint = "everything?q=search";
    // const { data, isLoading, error } = useFetch(endpoint, []);


    return (
        <div>
            The search input goes here
        </div>
    )
}
